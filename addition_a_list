// 👉 একটি list এর সব number যোগ করো

void main(){
  List <int> numbers = [10, 20, 30, 40];

  int count = 0;
  
  for(int i =0; i < numbers.length; i++){
    count = count + numbers[i];
  }
  
  print(count);
}
